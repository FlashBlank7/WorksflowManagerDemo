# 安全说明

## 先讲清楚这个工具的安全模型

guanjia 是**薄客户端**，它自己不做鉴权、不判权限——权限判定全在你的后端。
客户端这边只有三样东西值得关心：

### 1. 本地存的是会话令牌，明文

`~/.guanjia.json` 里存着后端签发的会话令牌（**密码从不落盘**）。
这个令牌的权限等同于你的账号：拿到它就能以你的身份用平台。

- 文件权限跟随你的 umask，**没有额外加密**
- 别把这个文件带出机器、别提交进仓库、别放进备份共享盘
- 令牌泄露了就去后端重新登录一次（会换发新令牌）

之所以不加密：本地加密需要一个存在别处的密钥，
而在单机场景里那个密钥最后也只能存在同一台机器上——只是让人误以为安全。

### 2. 网页壳没有登录

`guanjia web` 默认只监听 `127.0.0.1`，假设**能访问这个端口的人就是你**。

用 `--host` 放开到局域网时会强制生成一次性访问密钥（URL 带 `?k=`）。
说明白：那只是"别让同网段随手就能用"，**不是认证**——

- 密钥走 URL 与 Cookie，会留在浏览器历史和可能的代理日志里
- 没有速率限制，没有会话过期
- **公网暴露仍然不安全**

远程访问请走 SSH 端口转发（在远程机器上跑 `guanjia web` 时终端会直接印出命令）。

### 3. 一切能力在服务端执行

这是刻意的设计：智能体循环与每次工具调用都在后端并入审计台账，
**客户端无法伪造结果**。反过来说——客户端也无法限制后端能做什么，
工作流的权限边界由你的后端决定。

## 参考后端是「共享工作区」，不是多租户

上面说了权限边界由你的后端决定。那就把参考实现（自托管平台）
到底是什么模型讲清楚，别让人凭「有注册、有登录、有角色」自行想象：

**拿到注册令牌的人 = 平台的完全权限。**

2026-08-28 实测过一遍（新注册一个 member 账号去试）：

| 它能做什么 | 实测结果 |
| --- | --- |
| 看全部工作流 | 能，`/applications` 返回全部 15 个 |
| 看全局统筹总览 | 能 |
| 归档／运行／重建**别人建的**工作流 | 能（返回 404「找不到」而不是 403「没权限」） |

工作流上**没有属主字段**，服务端也没有按用户过滤——
账号体系区分的是「谁做的」（审计归因），不是「谁能做」。
`admin` 与 `member` 的区别只在少数管理接口。

所以：

- **注册令牌就是主密钥**，跟数据库密码同等对待，别贴进群里
- 不要拿它当多租户用——不同团队要隔离，就各自部署一套
- 要给人只读权限？现在做不到，别假设做得到

## 报告安全问题

发现问题请**不要开公开 issue**。发邮件到 `JohnYoung12138@gmail.com`，
带上：

- 影响什么（能读到什么、能做什么）
- 复现步骤（最小化的那种最好）
- 你用的版本（`guanjia --version`）与后端版本

我会尽快回复。如果你愿意留名，修复的提交信息里会致谢。

## 支持的版本

项目还在快速迭代，**只修最新版**。请先升级到最新再确认问题是否还在。

## 已知的边界

以下不是漏洞，是已知取舍，都写在[已知边界](docs/known-limits.md)里：

- 必须自托管后端，没有托管版
- 网页壳不做登录（见上）
- 令牌明文存放（见上）
- 会话历史不加密、不同步
- 产物下载经本地壳透传，内容会过一遍你的机器
- 参考后端是共享工作区，账号之间不隔离（见上）
