# Changelog

版本还未对外发布（PyPI 发布时从这里截取）。日期为完成日。

## 未发布 — 2026-08-29

这一天全在做同一件事：**把"用起来不对劲"的地方一个个揪出来**。
方法是逐条走用户真会撞到的路（打错命令、后端挂了、第一次安装、
后端形状不对），而不是读代码猜。

### 它不再突然说英文

- **状态码不给人看。** `guanjia run` 印的是 `run r1 · succeeded`、
  REPL 跟踪构建印 `building · 修订 3`、网页壳结果框印「状态 failed」。
  三个面各印各的，全是内部状态码。现在统一成中文说法（跑成了/没跑成/
  搭建中…），`--json` 那条路照给原状态码——脚本按它判，那是机器的接口。
  测试把三个面钉在一起：覆盖的状态集合要一致，同一个状态两边不能有两种叫法。
- **参数报错说人话。** `guanjia run` 不带参数时回的是 argparse 的英文用法
  加一个内部参数名（`name`）。现在：「还缺参数：name —— 工作流名字
  （支持唯一子串）或 id」，类型名也翻（float → 数字）。六个 parser 全换了，
  有测试扫全仓不许再有裸的 argparse.ArgumentParser。
- **打错子命令有得救。** 原本吐 argparse 英文用法；现在
  `guanjia tody` → 「没有「tody」这个命令。你是想说 today？」外加可用命令清单。
- **后端报错取 detail。** 原本第一行是 `remote 500: {"detail":"internal boom"}`。
  现在是「后端返回 500：<那句中文>」。
- **动作行显示中文名。** 界面上原本写着 `⚙ recent_runs → …`。

### 它不再一声不吭地误导

- **新用户不再被告知"登录态失效了"** —— 他一次都还没登录过。
  现在分岔：从没登录过就给完整的第一步（--login 做什么、服务器地址和
  注册令牌找谁要），登录过才说"失效"。
- **doctor 不再在没查调度器的情况下宣布"一切正常"。** 它原本只在
  已经有工作流误点时才查——可调度器刚死、还没到点时一个都不会误点。
- **doctor 不再给错方向。** 401 时它说"远端没有这个接口，多半是旧版本"，
  用户照着去升级后端，白费工夫。现在四种原因分开说。
- **契约自查不再声称"只读接口全齐"。** 表里只有 7 个，而客户端实际调 23 个；
  用自家的 examples/minimal_backend.py 一验，它缺 4 个接口却照样拿"全齐"。
  现在补到 16 个（需要 ID 的先从列表接口取样再探，全程只读），
  取不到样本的如实说"没验"，而且每条结论都说。

### 它不再把栈回溯甩给用户

- **入口兜底。** 后端返回的形状不对时，`guanjia today` 抛
  `KeyError: 'runs_today'` 加一整屏回溯。客户端里对远端返回值的直取下标有
  224 处，逐个改 .get() 既大又会吞掉真实信号——所以在入口兜一次，
  按三类说话：形状不对（指向 doctor --contract）、Ctrl-C（安静退出）、
  意料之外（给自查入口）。
  接线上差点栽了：兜底写在 _run_cli 里，而 pyproject 指的是 `main`——
  `python -m guanjia` 有兜底、装出来的 `guanjia` 命令没有，而用户用的正是后者。
- **报错要给出路。** 「没有档案「X」」不列现有档案、
  「不是合法 JSON：Expecting value: line 1 column 1」、
  「读不了文件：[Errno 2]…」——三条都改成说清位置并指出下一步。
- **找不到工作流时列候选。** 原本只有 `run` 会列，`export` 不会。

### 工程

- **补上 CI。** 仓里有 issue 模板、CONTRIBUTING、SECURITY，就是没有流水线——
  248 条测试从来只在本地跑过。现在 Python 3.10 和 3.13 各跑一遍
  （requires-python 写的是 >=3.10，只测一个版本那句承诺没人验过；
  3.10 上实跑过：247 过、1 跳过——跳的是缺可选依赖 pyyaml，与版本无关），
  外加一个盯住"零依赖"这个卖点的 job。

### 同机其他用户读不到你的东西了

这台开发机上还有别的账号在跑东西，一量才发现门都是开着的：

- **`~/.guanjia.json` 是 0644**，里面存着 API 令牌——同机谁都能读走、
  以你的身份操作平台。`write_text` 按 umask 建文件，默认 022 就是 644，
  不显式收就是松的。现在写的时候收 0600，**读的时候也收**：
  这次改动之前登录过的人只要不再改配置就永远走不到写那条路，
  而老文件名 `.bench.json` 我们只读不写，在写这一侧收多少次都轮不到它。
  已经比 0600 更严的（比如 0400）不动——用户可能是故意的。
- **网页壳回环也要访问密钥。** 原来的判断是"绑到回环之外才要"，
  理由是"能连到这个端口的人就是你"——在多用户主机上这句话不成立。
  钥匙第一次访问后种进 Cookie，书签照样能用；确定独占一台机器的可以
  `--no-key`，启动时会明说这么做的代价。

### 说得更准

- **`--server` / `--token` / `--profile` 每条命令都能用了。**
  `guanjia today --server 别的机器` 原先**一声不吭**地照旧查本机——
  today 和 doctor 压根不解析参数，敲什么都被默默丢掉。
  默默忽略比直接报错糟得多：报错用户会改，忽略他不会知道。
  顺手把 run/rerun/export/import 也补齐，帮助里写明。
- **地址写歪不再是"意料之外的问题"。** `--server 'http://[bad'` 原先打出
  「出了意料之外的问题：Invalid IPv6 URL」，还叫你去跑 `guanjia doctor`——
  你刚跑的就是它。现在说「服务器地址不对……要写成 http://主机:端口」，
  下一步也换成"改地址"而不是"确认后端启动了"。
- **失败清单那行字有歧义。** `✕ 某工作流 ×13 @2026-08-28T10:03:36` ——
  次数和一个具体时刻贴在一起，读起来像"那天失败了 13 次"。
  真值是"这个毛病前后一共 13 次，最近一次在那时"。三处出口
  （today、REPL、网页）统一措辞，时间也从 ISO 原文换成 `08-28 10:03`。
- **截了就说。** 面板一屏放 5 行失败、远端最多给 8 种，实际可能更多——
  两道截断一句说明都没有。现在多出来的种类会明说。
- **doctor：调度器活着不等于所有定时都开火。** 某个工作流每轮被跳过时
  照样打 ✓，等于替它瞒着。

### 自己的工具也要能响

- **契约检查漏查两个接口**（`/health` 和运行事件的 SSE 流），
  结论却写着「只读接口全齐——guanjia 能完整发挥」。只实现了
  `events/list`、没实现 `events` 的后端会通过检查，然后 `run --follow`
  当场不动。补上，并且**形状不对时不再说"能完整发挥"**——
  上面刚列完"读到一半会出错"，结论接一句"全齐"，读的人只会记住最后那句。
- **CI 补 `node --check`**：网页壳的 JS 没有构建步骤，也就没人替它看语法；
  语法一坏是整页白屏。冷路径脚本找 node 时也不再只看 PATH——
  这台机器上装着 node（前端就是它在跑），只是不在 PATH 上，
  于是那项检查一直显示"跳过"。
- **lint 扩到 `scripts` 和 `tests`**：scripts 里装的是冷路径自检、冒烟这些
  **用来验别人**的东西，它们自己坏了最没人发现。
- **中文 README 漏了 `rerun`**（英文那份有）。加了测试：每条命令都要在
  `--help` 和两份 README 里出现，反向也查——写了却不存在的命令更糟。

## 0.7.0 — 2026-08-28

这一版做两件事：**让它像个独立的项目**，以及**让它会收拾**。

### 独立性

- **不再要求你先学会我们的黑话。** 界面上原本到处是内部代号（构建智能体的
  内部名字、后端仓库名），开源用户根本不知道那是什么。现在只用描述性的说法；
  文档里指向另一个仓库的链接也全部去掉——那些在独立发布后都是死链。
- **首跑不再把人扔进登录表单。** 装完敲 `guanjia`，第一句原本是
  「服务器地址：」——而新用户既没有服务器，也没人告诉他去哪弄。
  现在先讲清楚它需要什么、怎么拿到，再问要不要现在填。
- **README 先说代价。** 「需要自托管后端」从第 60 行提到首屏；
  删掉了「不装直接试」这种做不到的承诺。新增「后端：它连的是什么」一节，
  列出 guanjia 实际依赖的全部 HTTP 接口——任何后端照着实现都能接。
- **视觉有了自己的样子。** 原本是通用面板相，而且品牌色与「成功」色是同一个绿，
  分不清哪块在表达状态。现在界面是墨与纸，**一见到饱和色就一定在说某件事的状态**；
  标识用 REPL 的提示符 `❯`，两张脸一眼同源。取舍写在 [design.md](docs/design.md)。
- 补齐 CONTRIBUTING（含「没有后端也能改大部分东西」的实测结论）、
  SECURITY（写真实的安全模型而不是套话）、issue 模板（必问 `doctor` 输出）。

### 会收拾了

搭建失败或中途放弃的工作流此前永远堆在列表里，没有任何清理路径——
真机上 71 个应用里 57 个从没发布过，其中 46 个连一次成功运行都没有。

- 体检式的建议：只挑「从没发布 + 从没成功跑过 + 放了一阵」的，三个条件都要满足
- 收起来只是从列表隐藏，**数据一条不删**，随时能拿回
- 对话里说「列表太乱了」「把 X 收起来」「拿回 X」；网页壳工具条有「收拾」和
  「已收起」，未发布的卡片悬停出现单个收起
- 动手前一律先问——工具不替用户决定哪些东西该清

### 顺手补的能力

- **改定时**：说「改成早上七点跑」就改，3 秒一步到位。此前没有这个工具，
  实测管家会绕十秒、调四个工具，最后什么也做不成
- **管家知道今天几号**了，不再从运行记录里倒推「昨天」
- **回答不再夹带推理过程**（「让我确认一下」这类是它的思考，不该给用户看）

### 平台侧（同期修复）

一轮针对性审计的 8 条实测缺陷全部修完，其中三条会真的伤到人：
SQLite 没有 busy_timeout，撞锁即判「工作流失败」（真实库里 122 条锁错误）；
调度器兜底记事失败会反杀循环，**定时任务全体静默停摆**；
体检把「正在跑/等人工确认」算成失败，进而**自动开修复构建花钱**。

## 0.6.2 — 2026-08-28

一轮"冷门路径"排查：把从没被执行过的分支逐个真跑一遍，18 条缺陷全部实测触发并修复。
起因是一处 `os` 未导入——回环启动走不到那段，线上一直没炸，用户在 SSH 里跑就崩。

**连不上远端时不再是一堆栈**

六个入口此前对"连不上"全是裸 traceback，而各处写好的「远端不可达」提示语反倒是死代码。
现在统一成 `RemoteUnreachable`，并区分三种情况：连不上 / 服务器答了错误码 /
**对面根本不是 guanjia 平台**（200 但正文不是 JSON——此前 `doctor` 会崩在
它唯一该诊断的场景里）。启动时平台没起来也不再把人推进登录流程。

**跟踪构建不被远端抖动打死**

搭建实测要 24 分钟 ≈ 288 次轮询一次不能落，而此前只接 `Ctrl+C`——远端抖一下
整个 REPL 就带栈退出。现在失败重试三次再放弃，放弃时说清"构建仍在远端进行"；
回答构建方的提问后转交失败时，答案原样回显而不是凭空蒸发。
`run --follow` 遇到不支持事件直播的远端会落回等待，不把已创建的运行丢在半路。

**对话不会因为存不下而丢失**

HOME 只读或磁盘满时，招牌 REPL 此前会在回答刚出来之后崩掉并带走整段对话；
现在说一句"没能存"继续用。

**网页壳启动与鉴权**

先能服务再印地址（此前先印一个能点的链接、后面跟着一段栈）；端口占用给人话和
换端口建议；`--host ::1` 从"白名单里写着却必崩"变成真能用（IPv6 地址族 + URL 方括号）；
非 ASCII 的访问密钥从"连接被重置"变成干净的 401。

**其它边界**：`import` 顶层不是对象 / 非 UTF-8 文件、`export -o` 指到写不了的路径，
都给人话和退出码 2 而不是 traceback。

**工程**：新增 `scripts/check_cold_paths.py`（22 项冷门环境自检，做过"把 bug 放回去
必须报错"的反向验证）；客户端测试 89 → 97。

## 0.6.1 — 2026-08-28

体检更准、建议更对、根因能查到底：

- **调度器健康**：平台 `GET /api/v1/scheduler/health` 报调度器的死活
  （上次轮询距今多久、轮询过几轮、最近错误）；`guanjia doctor` 在发现定时逾期时
  自动去查，把「去看看调度器还在不在」这句没法落地的建议换成事实——
  调度器活着就指向"定时配置改过没发布"，不正常就指向"重启平台/看 scheduler.failed 日志"
- **Web 统筹页平台状态条**：工作流都正常 ≠ 平台正常——调度器挂了定时任务会静默不跑，
  而「需要处理」区块要等到逾期才看得出来。状态条把远端与调度器的死活摆在最前面

- **定时静默失效检测**：此前只判「有定时但从没跑过」，判不出「跑过、然后悄悄不跑了」——
  这才是定时任务最常见的失效方式。按发布版快照的时刻表算出「上一次本该开火的时刻」
  与实际开火记录比对，宽限 90 分钟
- **「调用方式不对」不再算「工作流坏了」**：`missing required input` 这类失败
  不计入连败判定——否则任何人从 CLI 试跑忘带参数都会把工作流刷成 broken，
  还可能触发自动修复构建（真机上就是被冒烟脚本刷出来的）
- **建议按状态分流**：broken 给「说 X 坏了帮我修」，stale 给「先手动跑一次确认
  工作流没问题，再看调度器还在不在」——此前两种状态给同一条（对 stale 是错的）建议

## 0.6.0 — 2026-08-28

发现问题、说清原因、动手修好——这一轮把这条链闭合，并修掉一批
"看起来在正常工作"的缺陷。

**体检与自愈**

- **工作流体检**：平台 `GET /api/v1/health-report` 判 broken（窗口内全败 /
  最近连败≥3）/ stale（有定时却零运行）/ ok，带上最近一次失败原因摘要；
  四个入口同一份视图——`guanjia doctor`、`guanjia today`、Web 统筹页
  「需要处理」区块（点行直达详情）、对话里问「有什么坏了吗」
- **对话修复**：说一句「X 坏了帮我修」，构建智能体在**原工作流上**改（读现有草稿，
  不推倒重来），失败原因当线索，补上覆盖该故障的验收用例后重新发布。
  实测修好了一个已发布但 6 次全败的工作流，并找出了报错没指出的关联缺陷
  （实测记录见项目 wiki）
- **运行时间线与产物**：详情页每条运行可点开看全过程（节点耗时、错误标红），
  展开即列出该次运行落盘的文件，点击带令牌下载

**修掉的"看起来正常"的缺陷**

- **招牌 demo 静默算错**：多行示例被 `<input value>` 截成一行，业主提交后
  拿到 line_count=1 而不是 3——运行"成功"、结果是错的。表单改为按声明类型
  建控件、值走 DOM 赋值
- **声明 array 的输入从 CLI 100% 跑不通**：60 个声明输入里 32 个是 array，
  命令行传的永远是字符串。CLI 与网页壳现在共用同一套类型转换规则，
  解析失败就地报错而不是静默丢键
- **失败运行显示不出原因**：平台 `WorkflowRunState` 模型根本没有 error 字段，
  两侧却都在读 `state.error`。`today` 的失败列表、`guanjia run` 的结果、
  业主通道网页、管家的 `recent_runs` 现在都能说清为什么失败
- **管家跑工作流真机 500**：对 pydantic 模型调 `.get()`，招牌功能在生产里
  坏着没人发现（单测桩里那个字段恰好是 dict）
- **统计口径**：体检与总览此前把搭建期草稿自测当真实运行
  （真机 314 条里 255 条是自测），会把工作流误判 broken 并触发自动修复；
  定时任务改读发布版快照（真正开火的那份）
- 另有 evidence 状态恒 stale、事件列表取反导致长运行看不到结局、
  业主页用例计数恒显「1 / ? 通过」等

**工程**

- 两个真机冒烟脚本（管家工具 / CLI 子命令），都做过"把 bug 放回去必须报错"
  的反向验证——桩测不出平台真实返回什么形状
- 测试隔离护栏：任何用默认 `data_dir` 的测试自动改指 tmp
  （一个忘写隔离的测试曾打断线上在飞的构建）
- 公式引擎补 trim/split/count，同需求搭建时间 24.4min → 40s
  

## 0.5.0 — 2026-08-29

顺手到底：

- **失败重跑**：Web 失败行「重跑」就地反馈；CLI `guanjia rerun <run前缀>`
  （原输入透传，前缀在各应用近期运行里解析唯一命中）
- **`guanjia run --follow`**：实时滚动事件到 terminal 收尾再补 outputs；
  顺手修了 SSE 解析丢 `event:` 类型行的老 bug（`_event`/`_id`）
- **工作流搬运**：`guanjia export <名> [-o 文件|-]` ↔ `guanjia import <文件>
  [--name] [--no-publish]`，Web 详情「导出」/列表页「导入」同源；
  平台放行 `replace_workflow`/`replace_tests` 整片替换（全图校验，活体验证过）
- 工程：测试门加 pipefail（管道吞退出码教训）、桩测试增至 54 用例

## 0.4.0 — 2026-08-28

看得清、追得到：

- **运行时间线**：详情页每条运行点开看全过程（节点耗时、错误标红）；
  平台新增有界事件列表端点 `/runs/{id}/events/list`
- **失败可感知（三层）**：平台全局 `ALERT_WEBHOOK_URL`（fire-and-forget，
  告警绝不影响运行）· Web 在场红点+浏览器通知 · 日报工作流一句话自举
- **近 7 日趋势**：overview 按日聚合，`guanjia today` 符号行 + Web 成败堆叠柱
- **Web 档案命名**：手动填写连接时可起名保存

## 0.3.0 — 2026-08-28

围绕「装上就顺手」的一轮：

- **多远端档案**：`~/.guanjia.json` 升级 profiles 格式（旧格式自动迁移），
  `guanjia remote list/use/add/rm`、REPL `/remote`、Web 登录页档案下拉（免密切换，
  令牌失效退回密码登录）；`GUANJIA_PROFILE` 临时指定
- **`guanjia run <工作流> [k=v…] [--json] [--wait N]`**：脚本/cron 的一次性出口，
  名字唯一子串解析、tty 交互补参、退出码 0/1/2/3
- **`guanjia doctor`**：连接自诊断（配置→可达+延迟→登录态→会话存储），
  人话结论 + 下一步命令
- **Tab 补全**：`eval "$(guanjia completion bash)"`（zsh 同理）；`run` 后补
  工作流名、`remote use/rm` 后补档案名
- **桌面壳**：`guanjia web --app` 独立窗口（chromium 系探测）/`--open`；
  评估结论见 docs/decisions/desktop-shell.md——不引桌面依赖
- **顶层 `--help`**：完整命令地图
- **回归测试套**：31 用例零依赖 unittest（config/sessions/doctor/run/completion/help），
  真 HTTP 桩覆盖成功/失效/不可达三态

## 0.2.0 — 2026-08-27

Web 壳与 CLI 补齐到同一能力面（六切片）：

- 前端资产化：内嵌巨串拆成 guanjia/web/ 真文件（打包内加载）
- Web 对话流式（SSE 经本地壳透传，失败自动回退）
- 生成跟踪 + 提问应答：对话内构建卡片，构建时的提问就地作答
- 会话持久化：`~/.guanjia/sessions/`，CLI 与 Web 共享
- 对话 markdown 渲染（表格/代码/列表/加粗，先转义后变换）
- 体验 pass：智能滚动、防双发、焦点管理、窄屏顶栏

## 0.1.0 — 2026-08-26

首个能用的整体：CLI 对话管家（流式 + 构建跟随 + 在场应答）、Web 壳
（登录/注册、对话、统筹面板、工作流表单自动渲染）、`guanjia today`、
共享注册令牌 + 自助注册（首位注册者为管理员）、零依赖纯标准库薄壳。
