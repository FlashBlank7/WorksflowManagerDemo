# Changelog

版本还未对外发布（PyPI 发布时从这里截取）。日期为完成日。

## 0.6.2 — 2026-08-28

一轮"冷门路径"排查：把从没被执行过的分支逐个真跑一遍，18 条缺陷全部实测触发并修复。
起因是一处 `os` 未导入——回环启动走不到那段，线上一直没炸，用户在 SSH 里跑就崩。

**连不上远端时不再是一堆栈**

六个入口此前对"连不上"全是裸 traceback，而各处写好的「远端不可达」提示语反倒是死代码。
现在统一成 `RemoteUnreachable`，并区分三种情况：连不上 / 服务器答了错误码 /
**对面根本不是 guanjia 平台**（200 但正文不是 JSON——此前 `doctor` 会崩在
它唯一该诊断的场景里）。启动时平台没起来也不再把人推进登录流程。

**跟踪构建不被远端抖动打死**

搭建实测要 24 分钟 ≈ 288 次轮询一次不能落，而此前只接 `Ctrl+C`——远端抖一下
整个 REPL 就带栈退出。现在失败重试三次再放弃，放弃时说清"构建仍在远端进行"；
回答构建方的提问后转交失败时，答案原样回显而不是凭空蒸发。
`run --follow` 遇到不支持事件直播的远端会落回等待，不把已创建的运行丢在半路。

**对话不会因为存不下而丢失**

HOME 只读或磁盘满时，招牌 REPL 此前会在回答刚出来之后崩掉并带走整段对话；
现在说一句"没能存"继续用。

**网页壳启动与鉴权**

先能服务再印地址（此前先印一个能点的链接、后面跟着一段栈）；端口占用给人话和
换端口建议；`--host ::1` 从"白名单里写着却必崩"变成真能用（IPv6 地址族 + URL 方括号）；
非 ASCII 的访问密钥从"连接被重置"变成干净的 401。

**其它边界**：`import` 顶层不是对象 / 非 UTF-8 文件、`export -o` 指到写不了的路径，
都给人话和退出码 2 而不是 traceback。

**工程**：新增 `scripts/check_cold_paths.py`（22 项冷门环境自检，做过"把 bug 放回去
必须报错"的反向验证）；客户端测试 89 → 97。

## 0.6.1 — 2026-08-28

体检更准、建议更对、根因能查到底：

- **调度器健康**：平台 `GET /api/v1/scheduler/health` 报调度器的死活
  （上次轮询距今多久、轮询过几轮、最近错误）；`guanjia doctor` 在发现定时逾期时
  自动去查，把「去看看调度器还在不在」这句没法落地的建议换成事实——
  调度器活着就指向"定时配置改过没发布"，不正常就指向"重启平台/看 scheduler.failed 日志"
- **Web 统筹页平台状态条**：工作流都正常 ≠ 平台正常——调度器挂了定时任务会静默不跑，
  而「需要处理」区块要等到逾期才看得出来。状态条把远端与调度器的死活摆在最前面

- **定时静默失效检测**：此前只判「有定时但从没跑过」，判不出「跑过、然后悄悄不跑了」——
  这才是定时任务最常见的失效方式。按发布版快照的时刻表算出「上一次本该开火的时刻」
  与实际开火记录比对，宽限 90 分钟
- **「调用方式不对」不再算「工作流坏了」**：`missing required input` 这类失败
  不计入连败判定——否则任何人从 CLI 试跑忘带参数都会把工作流刷成 broken，
  还可能触发自动修复构建（真机上就是被冒烟脚本刷出来的）
- **建议按状态分流**：broken 给「说 X 坏了帮我修」，stale 给「先手动跑一次确认
  工作流没问题，再看调度器还在不在」——此前两种状态给同一条（对 stale 是错的）建议

## 0.6.0 — 2026-08-28

发现问题、说清原因、动手修好——这一轮把这条链闭合，并修掉一批
"看起来在正常工作"的缺陷。

**体检与自愈**

- **工作流体检**：平台 `GET /api/v1/health-report` 判 broken（窗口内全败 /
  最近连败≥3）/ stale（有定时却零运行）/ ok，带上最近一次失败原因摘要；
  四个入口同一份视图——`guanjia doctor`、`guanjia today`、Web 统筹页
  「需要处理」区块（点行直达详情）、对话里问「有什么坏了吗」
- **对话修复**：说一句「X 坏了帮我修」，构建智能体在**原工作流上**改（读现有草稿，
  不推倒重来），失败原因当线索，补上覆盖该故障的验收用例后重新发布。
  实测修好了一个已发布但 6 次全败的工作流，并找出了报错没指出的关联缺陷
  （实测记录见项目 wiki）
- **运行时间线与产物**：详情页每条运行可点开看全过程（节点耗时、错误标红），
  展开即列出该次运行落盘的文件，点击带令牌下载

**修掉的"看起来正常"的缺陷**

- **招牌 demo 静默算错**：多行示例被 `<input value>` 截成一行，业主提交后
  拿到 line_count=1 而不是 3——运行"成功"、结果是错的。表单改为按声明类型
  建控件、值走 DOM 赋值
- **声明 array 的输入从 CLI 100% 跑不通**：60 个声明输入里 32 个是 array，
  命令行传的永远是字符串。CLI 与网页壳现在共用同一套类型转换规则，
  解析失败就地报错而不是静默丢键
- **失败运行显示不出原因**：平台 `WorkflowRunState` 模型根本没有 error 字段，
  两侧却都在读 `state.error`。`today` 的失败列表、`guanjia run` 的结果、
  业主通道网页、管家的 `recent_runs` 现在都能说清为什么失败
- **管家跑工作流真机 500**：对 pydantic 模型调 `.get()`，招牌功能在生产里
  坏着没人发现（单测桩里那个字段恰好是 dict）
- **统计口径**：体检与总览此前把搭建期草稿自测当真实运行
  （真机 314 条里 255 条是自测），会把工作流误判 broken 并触发自动修复；
  定时任务改读发布版快照（真正开火的那份）
- 另有 evidence 状态恒 stale、事件列表取反导致长运行看不到结局、
  业主页用例计数恒显「1 / ? 通过」等

**工程**

- 两个真机冒烟脚本（管家工具 / CLI 子命令），都做过"把 bug 放回去必须报错"
  的反向验证——桩测不出平台真实返回什么形状
- 测试隔离护栏：任何用默认 `data_dir` 的测试自动改指 tmp
  （一个忘写隔离的测试曾打断线上在飞的构建）
- 公式引擎补 trim/split/count，同需求搭建时间 24.4min → 40s
  

## 0.5.0 — 2026-08-29

顺手到底：

- **失败重跑**：Web 失败行「重跑」就地反馈；CLI `guanjia rerun <run前缀>`
  （原输入透传，前缀在各应用近期运行里解析唯一命中）
- **`guanjia run --follow`**：实时滚动事件到 terminal 收尾再补 outputs；
  顺手修了 SSE 解析丢 `event:` 类型行的老 bug（`_event`/`_id`）
- **工作流搬运**：`guanjia export <名> [-o 文件|-]` ↔ `guanjia import <文件>
  [--name] [--no-publish]`，Web 详情「导出」/列表页「导入」同源；
  平台放行 `replace_workflow`/`replace_tests` 整片替换（全图校验，活体验证过）
- 工程：测试门加 pipefail（管道吞退出码教训）、桩测试增至 54 用例

## 0.4.0 — 2026-08-28

看得清、追得到：

- **运行时间线**：详情页每条运行点开看全过程（节点耗时、错误标红）；
  平台新增有界事件列表端点 `/runs/{id}/events/list`
- **失败可感知（三层）**：平台全局 `ALERT_WEBHOOK_URL`（fire-and-forget，
  告警绝不影响运行）· Web 在场红点+浏览器通知 · 日报工作流一句话自举
- **近 7 日趋势**：overview 按日聚合，`guanjia today` 符号行 + Web 成败堆叠柱
- **Web 档案命名**：手动填写连接时可起名保存

## 0.3.0 — 2026-08-28

围绕「装上就顺手」的一轮：

- **多远端档案**：`~/.guanjia.json` 升级 profiles 格式（旧格式自动迁移），
  `guanjia remote list/use/add/rm`、REPL `/remote`、Web 登录页档案下拉（免密切换，
  令牌失效退回密码登录）；`GUANJIA_PROFILE` 临时指定
- **`guanjia run <工作流> [k=v…] [--json] [--wait N]`**：脚本/cron 的一次性出口，
  名字唯一子串解析、tty 交互补参、退出码 0/1/2/3
- **`guanjia doctor`**：连接自诊断（配置→可达+延迟→登录态→会话存储），
  人话结论 + 下一步命令
- **Tab 补全**：`eval "$(guanjia completion bash)"`（zsh 同理）；`run` 后补
  工作流名、`remote use/rm` 后补档案名
- **桌面壳**：`guanjia web --app` 独立窗口（chromium 系探测）/`--open`；
  评估结论见 docs/desktop-shell.md——不引桌面依赖
- **顶层 `--help`**：完整命令地图
- **回归测试套**：31 用例零依赖 unittest（config/sessions/doctor/run/completion/help），
  真 HTTP 桩覆盖成功/失效/不可达三态

## 0.2.0 — 2026-08-27

Web 壳与 CLI 补齐到同一能力面（六切片）：

- 前端资产化：内嵌巨串拆成 guanjia/web/ 真文件（打包内加载）
- Web 对话流式（SSE 经本地壳透传，失败自动回退）
- 生成跟踪 + 提问应答：对话内构建卡片，构建时的提问就地作答
- 会话持久化：`~/.guanjia/sessions/`，CLI 与 Web 共享
- 对话 markdown 渲染（表格/代码/列表/加粗，先转义后变换）
- 体验 pass：智能滚动、防双发、焦点管理、窄屏顶栏

## 0.1.0 — 2026-08-26

首个能用的整体：CLI 对话管家（流式 + 构建跟随 + 在场应答）、Web 壳
（登录/注册、对话、统筹面板、工作流表单自动渲染）、`guanjia today`、
共享注册令牌 + 自助注册（首位注册者为管理员）、零依赖纯标准库薄壳。
